<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @article.body %>
</p>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>

<%= cl_image_tag @article.photo, height: 300, width: 400, crop: :fill %>
<%= cl_image_path @article.photo, height: 300, width: 400, crop: :fill %>


<%= image_tag @article.photo.url(:bright_face) %>
